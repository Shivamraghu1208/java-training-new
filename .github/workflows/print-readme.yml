name: Print README of Another Repository

on:
  push:

jobs:
  print_readme:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Fetch README content
        run: |
          readme_content=$(curl -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
                               -H "Accept: application/vnd.github.v3.raw" \
                               https://api.github.com/repos/Shivamraghu1208/git-traning/readme)
          echo "$readme_content"
